import React from "react";
import Footer from "./footer/Footer";

type Props = {};

const RouterInner = (props: Props) => {
  return (
    <div>
      <Footer />
    </div>
  );
};

export default RouterInner;
